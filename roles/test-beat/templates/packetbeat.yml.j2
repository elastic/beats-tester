# {{ ansible_managed }}

packetbeat.interfaces.device: {{ packetbeat_device }}

packetbeat.protocols:
- type: http
  ports: [80]

output.file:
  path: '${path.logs}'
  filename: output.json

logging:
  level: info
  json: true
  to_files: true
  files:
    name: {{ beat_name }}.json

http.host: localhost
